
export class Attributes {
    constructor(
        attributes = {} // Un objeto para almacenar atributos clave-valor
    ) {
        this.attributes = attributes; // Por ejemplo: { ramType: 'DDR4', capacity: '16GB', color: 'Black' }
    }
}
